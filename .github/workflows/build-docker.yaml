name: Build and Push Docker Images
run-name: Build and Push Docker Images
on:
  push:
    branches:
      - "*"
  workflow_dispatch:
## GITHUB_TOKEN permissions
permissions:
  id-token: write
  contents: read
  checks: write
  pull-requests: write
  issues: write

jobs:
  call-workflow:
    uses: DigitecGalaxus/actions/.github/workflows/reusable-docker-build-and-push.yml@feature/planetexpress/noissue-add-reusable-docker-build
    with:
      docker_image_tag: latest
      docker_bakefile_path: ./build/docker-bake.hcl
      docker_registry_name: "dgpublicimagesprod"
      environment: "Production"
